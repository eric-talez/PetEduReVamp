Okay, the script element contains JavaScript code that handles communication with an iframe and manages page navigation and third-party cookie checks. The previous sibling of the script is the iframe with id="iframe". The 404 error and the console logs are most likely related to the content loaded into the iframe or the URL used to load the iframe. The script is responsible for setting the iframe.src. It's likely that the initial path or a path within the iframe is incorrect or inaccessible, resulting in the 404 error. Let's try to determine the path used to set the iframe.src and how the initialPath parameter is used. We also need to know the URL of the page.


Code executed

const data = {
    pageUrl: window.location.href,
    iframeSrc: document.getElementById('iframe')?.src,
    scriptContent: $0.textContent,
};
Data returned
{"pageUrl":"https://03f1db47-25ff-4329-b620-7337c36e0625-00-19h1hay5khk9t.picard.replit.dev/__replco/workspace_iframe.html?initialPath=%2F&id=%3Ar1l7%3A","iframeSrc":"https://03f1db47-25ff-4329-b620-7337c36e0625-00-19h1hay5khk9t.picard.replit.dev/","scriptContent":"\n      const iframe = document.getElementById('iframe');\n      const urlParams = new URLSearchParams(window.location.search);\n      const initialPath = urlParams.get('initialPath') || '/';\n      const webviewId = urlParams.get('id') || null;\n\n      function postMessageToWorkspace(data) {\n        const dataWithPaneId = { ...data, id: webviewId };\n\n        parent?.postMessage(dataWithPaneId, 'https://replit.com');\n        parent?.postMessage(dataWithPaneId, 'https://staging.replit.com');\n        parent?.postMessage(dataWithPaneId, 'https://firewalledreplit.com/');\n        parent?.postMessage(dataWithPaneId, 'https://staging.firewalledreplit.com/');\n        parent?.postMessage(dataWithPaneId, 'http://localhost:3000');\n        \n      }\n\n      window.addEventListener('message', (event) => {\n        if (\n          typeof event.data !== 'object' ||\n          event.data === null ||\n          !('type' in event.data)\n        ) {\n          \n          return;\n        }\n\n        if (event.origin === window.location.origin) {\n          \n          postMessageToWorkspace(event.data);\n\n          return;\n        }\n\n        \n        iframe.contentWindow.postMessage(event.data, window.location.origin);\n      });\n\n      function canAccessIframeWindow() {\n        try {\n          iframe.contentWindow.document.body;\n          return true;\n        } catch (err) {\n          return false;\n        }\n      }\n\n      \n      \n      \n      \n      async function checkThirdPartyCookie() {\n        let enabled = false;\n        if ('hasStorageAccess' in document) {\n          enabled = await document.hasStorageAccess();\n        }\n\n        if (!enabled) {\n          const testCookieName = '__replit_third_party_cookie_test__';\n          document.cookie = `${testCookieName}=1;`;\n          enabled = document.cookie.indexOf(testCookieName) !== -1;\n          \n          document.cookie = `${testCookieName}=1; max-age=0;`;\n        }\n        \n        postMessageToWorkspace({\n          type: 'third-party-cookie-check',\n          payload: {\n            enabled\n          }\n        });\n      }\n\n      function postLocation() {\n        if (!canAccessIframeWindow()) {\n          postMessageToWorkspace({\n            type: 'page-navigation',\n            payload: {\n              isExternal: true,\n            },\n          });\n\n          return;\n        }\n\n        postMessageToWorkspace({\n          type: 'page-navigation',\n          payload: {\n            isExternal: false,\n            relativeUrl:\n              iframe.contentWindow.location.pathname +\n              iframe.contentWindow.location.search +\n              iframe.contentWindow.location.hash,\n          },\n        });\n      }\n      function onIframeLoad() {\n        \n        \n        postLocation();\n        \n        \n        checkThirdPartyCookie();\n\n        if (canAccessIframeWindow()) {\n          \n          const script = document.createElement('script');\n          script.type = 'text/javascript';\n          script.src = 'https://replit.com/public/js/beacon.js';\n          iframe.contentDocument.head.appendChild(script);\n          \n          \n          iframe.contentWindow.addEventListener('popstate', postLocation);\n          iframe.contentWindow.addEventListener('hashchange', postLocation);\n          iframe.contentWindow.addEventListener('__replit-history-event', postLocation);\n        }\n      }\n      iframe.addEventListener('load', onIframeLoad);\n      iframe.src = new URL(initialPath, window.location.origin).href;\n    "}
Use code snippets with caution

The 404 error "페이지를 찾을 수 없습니다" (Page Not Found) is occurring because the iframe is loading the root path / of the domain https://03f1db47-25ff-4329-b620-7337c36e0625-00-19h1hay5khk9t.picard.replit.dev/, and there is no content available at that location. The initialPath URL parameter is correctly set to /, which is then used to build the iframe.src. The problem is that this root path does not serve any content. The console logs are related to authentication logic and are not directly causing the 404 error. The problem is that the iframe is trying to load a page that doesn't exist on the server.

Possible Solutions:

Provide Content at the Root Path: The most direct solution is to ensure that the server at https://03f1db47-25ff-4329-b620-7337c36e0625-00-19h1hay5khk9t.picard.replit.dev/ serves content when a request to / is made. This could be a basic HTML page or a redirect to a valid path.
Change the initialPath Parameter: A quick workaround is to modify the initialPath parameter in the URL of the main page. You could change the URL to https://03f1db47-25ff-4329-b620-7337c36e0625-00-19h1hay5khk9t.picard.replit.dev/__replco/workspace_iframe.html?initialPath=/someValidPath&id=%3Ar1l7%3A (replacing /someValidPath with a real, valid path that exists on the server).