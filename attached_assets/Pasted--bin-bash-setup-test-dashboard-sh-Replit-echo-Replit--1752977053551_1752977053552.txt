#!/bin/bash
# setup-test-dashboard.sh - Replit 테스트 대시보드 설정

echo "🎯 Replit 테스트 대시보드 설정을 시작합니다..."

# 1. 테스트 대시보드 디렉토리 생성
mkdir -p test-dashboard/public
mkdir -p test-dashboard/views

# 2. 필요한 패키지 설치
echo "📦 필요한 패키지 설치 중..."
npm install express ejs node-fetch @types/express --save-dev

# 3. 메인 대시보드 서버 파일 생성
cat > test-dashboard-server.ts << 'EOF'
// test-dashboard-server.ts - 간단한 테스트 대시보드 서버
import express from 'express';
import { exec } from 'child_process';
import { promisify } from 'util';
import fs from 'fs/promises';
import path from 'path';

const execAsync = promisify(exec);

const app = express();
app.use(express.json());
app.use(express.static('test-dashboard/public'));

// 테스트 결과 저장소
let testResults = {
  lastRun: null,
  isRunning: false,
  suites: {
    'code-quality': {
      name: '코드 품질 검사',
      status: 'pending',
      tests: [
        { name: 'TypeScript 컴파일', status: 'pending', message: '대기 중' },
        { name: 'ESLint 검사', status: 'pending', message: '대기 중' },
        { name: '의존성 검사', status: 'pending', message: '대기 중' }
      ]
    },
    'server-functions': {
      name: '서버 기능 테스트',
      status: 'pending',
      tests: [
        { name: '서버 시작', status: 'pending', message: '대기 중' },
        { name: '기본 라우트', status: 'pending', message: '대기 중' },
        { name: '헬스체크', status: 'pending', message: '대기 중' }
      ]
    },
    'api-integration': {
      name: 'API 연동 테스트',
      status: 'pending',
      tests: [
        { name: '외부 API 호출', status: 'pending', message: '대기 중' },
        { name: '응답 검증', status: 'pending', message: '대기 중' },
        { name: '에러 처리', status: 'pending', message: '대기 중' }
      ]
    }
  }
};

// 메인 대시보드 페이지
app.get('/test-dashboard', (req, res) => {
  res.send(`
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🎯 Replit 테스트 대시보드</title>
    <style>
        body { 
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            margin: 0; padding: 20px; background: #f5f5f5; 
        }
        .container { max-width: 1200px; margin: 0 auto; }
        .header { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white; padding: 30px; border-radius: 15px; margin-bottom: 30px;
            text-align: center;
        }
        .controls { text-align: center; margin: 30px 0; }
        .btn {
            padding: 12px 24px; margin: 0 10