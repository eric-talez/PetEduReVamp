// 이 파일은 임시 브릿지 파일입니다.
// 기존 코드에서 SimpleApp.tsx에서 useAuth를 가져오는 것을 지원하기 위한 임시 조치입니다.
// 실제 useAuth 로직은 hooks/useAuth.tsx에 있습니다.

import { useAuth, AuthProvider, UserRole } from '../hooks/useAuth';

export { useAuth, AuthProvider, UserRole };